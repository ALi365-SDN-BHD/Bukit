- [x] `SanitizeFileSegment` uses fixed cross-platform invalid char set, not `Path.GetInvalidFileNameChars()`
- [x] `SanitizeFileSegment_RemovesInvalidChars` expects `"hello_world__test.txt"` on all platforms
- [x] `MakeAbsolute_ReturnsRootedPathUnchanged` renamed and uses cross-platform path assertions
- [x] `NotionBlocksRenderer` uses `\n` instead of `AppendLine` (7 occurrences)
- [x] `TableBlockRenderer` uses `\n` instead of `AppendLine` (2 occurrences)
- [x] `ProcessPluginInvoker.CopyAllowedEnvironment` uses case-insensitive lookup
- [x] `ProtocolEchoPlugin` `env-allowlist` mode falls back HOME → USERPROFILE on Windows
- [x] `ExternalProtocolPluginTests` path assertions use JSON parsing instead of string-contains
- [x] `dotnet test bukit.slnx -c Release` passes on macOS, no regressions
- [x] `bash scripts/smoke.sh Release` passes on macOS
