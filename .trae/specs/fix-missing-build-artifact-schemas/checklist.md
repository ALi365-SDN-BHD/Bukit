# Checklist
- [x] `docs/schemas/artifact-manifest.v1.schema.json` exists and is valid JSON Schema (draft 2020-12)
- [x] `docs/schemas/release-bundle-checksums.v1.schema.json` exists and is valid JSON Schema (draft 2020-12)
- [x] `docs/schemas/build-manifest-digest.v1.schema.json` exists and is valid JSON Schema (draft 2020-12)
- [x] Each schema declares correct `$id` matching its file name (e.g., `https://bukit.dev/schemas/artifact-manifest.v1.json`)
- [x] Each schema declares correct `$schema` (`https://json-schema.org/draft/2020-12/schema`)
- [x] Each schema includes `schema` property with `const` matching its `$id`
- [x] Each schema includes `schemaVersion` property with `const: "1.0"`
- [x] Each schema sets `additionalProperties: false` at root level
- [x] `artifact-manifest.v1.schema.json` matches `WriteArtifactManifest` output: required fields `schema`, `schemaVersion`, `generatedAt`, `artifactCount`, `artifactSetHash`, `artifacts`
- [x] `release-bundle-checksums.v1.schema.json` matches `WriteReleaseBundleChecksums` output: required fields `schema`, `schemaVersion`, `fileCount`, `bundleHash`, `files`
- [x] `build-manifest-digest.v1.schema.json` matches `WriteBuildManifestDigest` output: required fields `schema`, `schemaVersion`, `generatedAt`, `reportCount`, `reportSetHash`, `artifactManifestHash`, `releaseBundleHash`, `securityReportHash`, `reports`
- [x] `bash scripts/build-repro.sh` passes without schema-related errors (all three `validate-artifacts-json.sh` invocations pass with "OK artifact schema validation")
- [x] No `ERROR: * declares ... but ... is missing` output in validation run
- [x] Pre-existing `security-report.v1.schema.json` fixed: added `externalPlugins` property and corrected `policy` enum from `["allow", "deny"]` to `["allow", "deny", "warn"]`
