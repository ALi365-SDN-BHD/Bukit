# External Plugin Protocol Labs Note

The external process protocol is implemented in Core libraries, but individual
plugin commands are project capabilities exposed through plugin config and
manifests. They are not part of the static Core command table.

When documenting a concrete plugin, cite its manifest, config, permissions,
entry platform, hash verification, and protocol messages.
