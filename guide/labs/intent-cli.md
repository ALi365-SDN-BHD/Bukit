# Intent Workflow Labs Note

Intent-driven site generation is Labs material. Stable Core config is
`site.yaml`; use `guide/user/04-site-yaml-config.md` for the current contract.

Do not present intent files as required for Core builds.
