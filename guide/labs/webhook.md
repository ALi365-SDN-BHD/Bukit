# Webhook Labs Note

Webhook services are not part of the static Core command registry. A webhook
workflow may trigger a Core build externally, but the webhook service itself is
outside stable Core documentation.
