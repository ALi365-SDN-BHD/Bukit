# Import HTML Demo Labs Note

HTML demo import is not part of the static Core command registry. Treat import
as a Labs or external plugin workflow.

Core can build the resulting site when the workflow produces valid `site.yaml`,
content sources, themes, templates, and assets.
