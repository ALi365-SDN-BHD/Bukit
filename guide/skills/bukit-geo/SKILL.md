---
name: bukit-geo
description: Use for GEO, llms.txt, llms-full.txt, AI bot mode, and geo audit.
---

# Bukit GEO

GEO config lives under `site.seo.geo`. Outputs include `llms.txt`,
`llms-full.txt`, and `.bukit/geo-report.json`.

Use `bukit geo audit --dir dist` after builds.
