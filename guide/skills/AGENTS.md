# Bukit Core Skill Red Lines

- This file applies only below `guide/skills/` and may tighten, but never weaken, the root red lines.
- Treat `src/Bukit-Core/Bukit.Cli/Cli/BukitCliSpecs.cs` as the Core command source and `src/Bukit-Core/Bukit.Config/AppConfig.cs` as the config-field source.
- Do not teach non-Core command families as stable Core commands.
- Do not use Labs skills without an explicit Labs or experimental request.
