---
name: bukit-dev
description: Use for the LiveReload development server, file watching, LAN safety, and dev server errors.
---

# Bukit Dev

`dev` builds, serves, watches, and reloads. Defaults are host `localhost` and
port `35729`. Use `--allow-lan` or `--public` only on trusted networks.
