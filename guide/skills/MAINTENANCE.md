# Skills Maintenance

When Core command, config, route, template, or output contracts change:

1. Update the relevant skill.
2. Update `skills-index.yaml`.
3. Run `bash guide/skills/scripts/validate-skills-strict.sh`.
4. Run `bash scripts/checks/skills-schema.sh`.
