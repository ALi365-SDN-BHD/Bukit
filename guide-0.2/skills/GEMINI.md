# Bukit Core 1.0 Skills

Activate `using-bukit` for Bukit tasks. Treat `bukit-cli-reference` as the command source of truth and `bukit-config` as the background for every `site.yaml` change.

Do not route clone/import/webhook/theme registry/theme wizard/template tools through the Core gateway.
