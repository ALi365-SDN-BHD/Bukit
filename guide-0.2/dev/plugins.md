# Plugins

Core plugin documentation has moved to [Built-in Plugins](built-in-plugins.md).

Bukit Core 1.0 only documents the built-in plugin registry. Runtime loading and protocol workflows belong in Labs documentation and must not be presented as default Core behavior.
